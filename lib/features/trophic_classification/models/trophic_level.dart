/// Pedagogical trophic levels used in the Classification mode.
///
/// Only four values — "top predator" is a contextual badge, not a fifth level.
/// This enum does not store colours, icons, HEX values or UI text.
enum TrophicLevel {
  producer,
  primaryConsumer,
  secondaryConsumer,
  tertiaryConsumer,
}
